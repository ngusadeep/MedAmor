[project]
name = "interactive-fhir-browser"
version = "0.1.0"
description = "Interactive terminal browser for FHIR patient data"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[project.scripts]
fhir-browser = "main:main"

[dependency-groups]
dev = []
